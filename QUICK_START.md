# 🚀 Quick Start Guide

## เริ่มต้นใช้งานด่วน (5 นาที)

### 1️⃣ ติดตั้ง Dependencies

```bash
cd helpdesk-thailand
npm install
```

### 2️⃣ ตั้งค่าฐานข้อมูล

แก้ไข `.env.local`:

```env
DATABASE_URL="postgresql://postgres:password@localhost:5432/helpdesk"
```

### 3️⃣ สร้าง Database

```bash
# สร้าง database schema
npx prisma db push

# เพิ่มข้อมูลทดสอบ (optional)
npm run db:seed
```

### 4️⃣ รันโปรเจค

```bash
npm run dev
```

เปิด: **http://localhost:3000**

---

## ✅ Checklist

- [ ] ติดตั้ง PostgreSQL
- [ ] สร้าง database `helpdesk`
- [ ] แก้ไข `.env.local`
- [ ] รัน `npm install`
- [ ] รัน `npx prisma db push`
- [ ] รัน `npm run dev`
- [ ] เปิด http://localhost:3000

---

## 🎯 ทดสอบระบบ

### สร้าง Ticket แรก

1. ไปที่: http://localhost:3000/tickets/new
2. กรอกข้อมูล:
   - ชื่อ: สมชาย ทดสอบ
   - เบอร์: 0812345678
   - หัวข้อ: ทดสอบระบบ
   - รายละเอียด: นี่คือ ticket ทดสอบแรก
   - ความสำคัญ: ปานกลาง
3. คลิก "สร้าง Ticket"

### ดู Dashboard

ไปที่: http://localhost:3000/dashboard

### ดูรายการ Tickets

ไปที่: http://localhost:3000/tickets

### เปิด Prisma Studio

```bash
npx prisma studio
```

เปิด: http://localhost:5555

---

## 📖 เอกสารเพิ่มเติม

- **README.md** - ภาพรวมโปรเจค
- **SETUP_GUIDE.md** - คำแนะนำติดตั้งแบบละเอียด
- **PROJECT_STRUCTURE.md** - โครงสร้างโปรเจค

---

## 🆘 เจอปัญหา?

### ฐานข้อมูลเชื่อมต่อไม่ได้

```bash
# ตรวจสอบว่า PostgreSQL running
# Windows: Services > PostgreSQL
# Mac: brew services list
# Linux: sudo systemctl status postgresql
```

### Port 3000 ถูกใช้

```bash
PORT=3001 npm run dev
```

### Prisma ไม่ทำงาน

```bash
rm -rf node_modules
npm install
npx prisma generate
```

---

## 🎉 เสร็จแล้ว!

ระบบพร้อมใช้งาน! ทดลองสร้าง Tickets และสำรวจ features ต่างๆ

สนุกกับการใช้งานครับ! 🙂
