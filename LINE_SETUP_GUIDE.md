# 📱 LINE Integration Setup Guide

คู่มือการตั้งค่าการเชื่อมต่อ LINE Messaging API สำหรับระบบ Help Desk

---

## 📋 สิ่งที่ต้องเตรียม

- LINE Account
- เบอร์โทรศัพท์สำหรับยืนยันตัวตน
- Email

---

## 🚀 ขั้นตอนการตั้งค่า

### Step 1: สร้าง LINE Developers Account

1. ไปที่: https://developers.line.biz/console/
2. Login ด้วย LINE account
3. คลิก **"Create a new provider"**
   - Provider name: `Thailand Post` หรือชื่อบริษัท/โปรเจกต์
   - คลิก **Create**

### Step 2: สร้าง Messaging API Channel

1. ใน Provider ที่สร้าง คลิก **"Create a new channel"**
2. เลือก **"Messaging API"**
3. กรอกข้อมูล:
   - **Channel name**: `Help Desk Bot`
   - **Channel description**: `Thailand Post Help Desk Notification System`
   - **Category**: `Productivity`
   - **Subcategory**: `Business productivity`
   - **Email**: อีเมลติดต่อ
4. อ่านและยอมรับ **Terms of Use**
5. คลิก **"Create"**

### Step 3: ตั้งค่า Channel

1. ไปที่ tab **"Messaging API"**
2. Scroll ลงมาหาส่วน **"Channel access token"**
3. คลิก **"Issue"** button
4. **คัดลอก token** ที่ได้ (เริ่มต้นด้วย `ey...` หรือตัวอักษรยาวๆ)
5. เก็บ token ไว้อย่างปลอดภัย

### Step 4: ตั้งค่า Bot

1. ใน tab **"Messaging API"** scroll ลงมาหา:
   - **"Use webhooks"**: ปิด (Disabled) - ตอนนี้ยังไม่ต้องใช้
   - **"Allow bot to join group chats"**: เปิด (Enabled) ⭐ สำคัญ!
   - **"Auto-reply messages"**: ปิด (Disabled)
   - **"Greeting messages"**: ปิด (Disabled)

2. ใน tab **"Basic settings"** จด:
   - **Bot basic ID**: `@xxxxxxxxx` (จะใช้ค้นหา Bot)

### Step 5: สร้าง LINE Group และเพิ่ม Bot

1. เปิดแอป LINE บนมือถือ
2. สร้างกลุ่มใหม่:
   - ชื่อกลุ่ม: `Help Desk Test` (ตั้งชื่ออะไรก็ได้)
   - เพิ่มสมาชิก: เฉพาะคนที่ต้องการให้เห็น notification

3. เชิญ Bot เข้ากลุ่ม:
   - คลิก เมนู ≡ → เพิ่มเพื่อน
   - ค้นหาด้วย **Bot Basic ID** (เช่น `@123abcde`)
   - หรือสแกน QR Code จาก Messaging API console
   - เชิญ Bot เข้ากลุ่ม

4. ทดสอบ Bot:
   - พิมพ์ `สวัสดี` ในกลุ่ม
   - Bot จะไม่ตอบกลับ (เพราะเราปิด auto-reply)
   - แสดงว่า Bot เข้ากลุ่มสำเร็จ ✅

### Step 6: หา Group ID

**วิธีที่ 1: ใช้ LINE Bot Designer (แนะนำ)**

1. ไปที่ Messaging API console
2. คลิก **"Messaging API"** tab
3. Scroll หา **"Send test message"**
4. เลือก **"Chat room"** → เลือกกลุ่มที่สร้าง
5. **คัดลอก Group ID** (รูปแบบ: `Cxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`)

**วิธีที่ 2: ใช้หน้าทดสอบในระบบ**

1. เปิดระบบ Help Desk: http://localhost:3000/line-test
2. ใส่ Channel Access Token และลอง Send Test
3. ดู error message จะบอก Group ID (ถ้ามี)

### Step 7: อัปเดต Environment Variables

1. เปิดไฟล์ `.env` ในโปรเจกต์
2. อัปเดตค่าเหล่านี้:

```env
# LINE Messaging API Configuration
LINE_CHANNEL_ACCESS_TOKEN="eyJhbGciOiJIUzI1NiJ9..."  # Token จาก Step 3
LINE_DEFAULT_GROUP_ID="Cxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"  # Group ID จาก Step 6
```

3. บันทึกไฟล์
4. Restart dev server:
```bash
npm run dev
```

---

## 🧪 ทดสอบการเชื่อมต่อ

### ทดสอบด้วยหน้าเว็บ

1. เปิด: http://localhost:3000/line-test
2. กรอก Group ID
3. คลิก **"ส่งข้อความธรรมดา"**
4. ตรวจสอบในกลุ่ม LINE → ควรได้รับข้อความ ✅

5. คลิก **"ส่ง Flex Message"**
6. ควรเห็นข้อความสวยๆ แบบ card ✅

### ทดสอบด้วยการสร้าง Ticket

1. เปิด: http://localhost:3000/tickets/new
2. กรอกข้อมูลและสร้าง Ticket
3. ตรวจสอบกลุ่ม LINE → ควรได้รับ notification แบบ Flex Message พร้อมข้อมูล Ticket ✅

---

## ❓ Troubleshooting

### 1. ส่งข้อความไม่สำเร็จ (Error 401)

**สาเหตุ**: Channel Access Token ไม่ถูกต้อง

**แก้ไข**:
- ตรวจสอบ token ใน `.env` ว่าถูกต้องหรือไม่
- ลอง Issue token ใหม่จาก LINE Developers Console
- ตรวจสอบว่าไม่มีช่องว่างหรือตัวอักษรเกินมา

### 2. ส่งข้อความไม่สำเร็จ (Error 400)

**สาเหตุ**: Group ID ไม่ถูกต้อง

**แก้ไข**:
- ตรวจสอบว่า Bot เข้ากลุ่มแล้วหรือยัง
- ลอง kick Bot ออกแล้วเชิญเข้าใหม่
- ตรวจสอบ Group ID ว่าถูกต้องหรือไม่ (ต้องขึ้นต้นด้วย `C`)

### 3. Bot ไม่สามารถเข้ากลุ่มได้

**สาเหตุ**: ยังไม่เปิด "Allow bot to join group chats"

**แก้ไข**:
1. ไปที่ Messaging API console
2. เปิด **"Allow bot to join group chats"**
3. Save settings
4. ลองเชิญ Bot เข้ากลุ่มอีกครั้ง

### 4. ได้รับข้อความแต่เป็น plain text แทน Flex Message

**สาเหตุ**: Flex Message structure ไม่ถูกต้อง

**แก้ไข**:
- ตรวจสอบ console logs มี error อะไรหรือไม่
- ลองส่ง test Flex Message จากหน้า /line-test

---

## 📊 Free Tier Limits

LINE Messaging API Free Plan มีข้อจำกัด:

- **500 messages/month** ฟรี
- หลังจากนั้น: **~฿0.30/message**

**💡 Tips**:
- ใช้สำหรับ production จริงๆ เท่านั้น
- Development ใช้ console.log แทนการส่งข้อความ
- Monitor usage ที่ LINE Developers Console

---

## 🎯 Next Steps

หลังจากตั้งค่าเสร็จแล้ว:

1. ✅ ทดสอบส่งข้อความสำเร็จ
2. ✅ สร้าง Ticket ใหม่และได้รับ notification
3. 📝 เพิ่ม LINE notification สำหรับ Ticket Resolved
4. 📝 เพิ่ม SLA warning notifications
5. 📝 Customize Flex Message template ตามต้องการ

---

## 📚 Resources

- [LINE Messaging API Docs](https://developers.line.biz/en/docs/messaging-api/)
- [Flex Message Simulator](https://developers.line.biz/flex-simulator/)
- [LINE Bot Designer](https://developers.line.biz/console/)

---

## 🆘 Need Help?

หากมีปัญหาหรือคำถาม:

1. ตรวจสอบ console logs (`npm run dev`)
2. ลองส่ง test message ที่ /line-test
3. ตรวจสอบ LINE Developers Console → Messaging API → Error logs

สนุกกับการพัฒนา! 🚀
